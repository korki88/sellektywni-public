Future<void> downloadPdfWithAuth({
  required String url,
  required String bearerToken,
  required String filename,
}) async {
  throw UnsupportedError('Pobieranie PDF jest dostępne w przeglądarce (Flutter Web).');
}
